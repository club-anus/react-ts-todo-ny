import * as React from 'react';
import './App.css';
import TodoList from './component/TodoList'

class App extends React.Component {
  public render() {
    return (
      <TodoList/>
    );
  }
}

export default App;
